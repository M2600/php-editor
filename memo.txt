セキュリティ設定


x ユーザ入力のうちパスに使われる文字列をbasename()で安全化
    ファイル名をbasename()で
    ユーザIDをbasename()で安全化
    ../../と言ったパストラバーサル対策

+ ディレクトリのアクセス制限
    nginxの設定?

+ phpによるシェルコマンド実行等の対策
    php側で制限できるが、本プロジェクトのみ対策したいのでプログラム側で対応
    
    聞く
    特定関数の呼び出しをリプレイス？
    特定関数を呼び出していると保存、実行させない？

    実行用phpを生成し、exec("php -c php.ini " . $path)とか実行。
    php.iniに禁止関数を設定しておけば、安全に実行できる。
    ユーザの作成したphpファイルの名前に注意。

    https://gist.github.com/mccabe615/b0907514d34b2de088c4996933ea1720


    + php workerを複数生成、
        pool.d/*.confにphp.iniの項目を記述可
        動作はうまく言ってそうなので、設定項目を煮詰める



- クラスdir
    最終的に必要になる
    クラス管理機能、パスワードの登録等も

    bitarrow 
        教員登録　ー＞　教員がクラスを作っていく

        bitarrowではクラス名は名前空間が全体で同じ、
        教員メニュー
            ユーザ一覧
                パスワード参照
                代理ログイン: ユーザサポート等、現状ちょっと危ないログアウトしたりしてもログインが残る
            ユーザ登録
                登録フォーム、csv一括登録(文字コードが若干厄介。bitarrowだと)
            ユーザの全ファイルダウンロード
                全ユーザのファイルをzipでダウンロードできる、採点などに使える
            クラス設定
                パスワードの必須
                ユーザ登録の権限
            ユーザアクティビティ
                時間指定でユーザの動向を確認できる
                実行回数、エラー回数、最終実行からの経過時間、今実行しているファイル、アクティビティ（SURESSERRみたいに文字で表示各時点での状況、２つの時点でのプログラムをdiffで表示、比較できる。）

            研究目的で使うなら分析用ログを残しておくと良い

    2024後期はほか授業で使う予定なし
    現状は作らなくて良い

    bitarrowと連携させるとか
    saccessと連携させるとか
        ユーザ情報はそっちに投げる

    クラス内ファイル共有
        あると良い

+ 素材アップロード、プレビュー機能

+ jsのfetchを連投すると送信と受信が入れ替わるかも
    jsでリクエストとレスポンスの対応をチェックする必要あり
    http3通信の仕様上順序は入れ替わるかもしれないが、リクエストに対するレスポンスが入れ替わることはない。
    レスポンスを処理する際の実装を気をつければ大丈夫
    本システムではファイル喪失の報告はなし

    とりあえずの対策として、ログファイルに時間、ファイル内容を出力
    ファイルごとにAce-editorをつくる。切り替え
    ace-editorオブジェクトごとにサーバー接続の情報も持たせる


- 授業利用バグ
    + ダイアログ系がかぶる
    + 別ページで表示がたまに機能しなくなる
        多分windows.closed==trueのときにおかしくなる
    カーソル位置を表示の位置がずれ


- アップロードが目立ちすぎ
    ファイルメニューを作る？
        他に追加項目は？
    新規ボタンをおおきくする？


- autocompletionのフリーズ
+ カーソルをキーボードで動かしたときにページガスクロールされない
+ ctrl p のキーを開放


+ phpエディタのCORS除外にhttps://klabst.eplang.jpを追加








PHPエディタ
	先生だけができる
		学生になりすましたり、学生のファイルを覗き見る機能
		先生からは編集できないようにする。実行はできる。実行時にファイルが書き換わるのはよし。

+クッキーのセキュリティ
    ユーザが作成したページでも開発ページと同じセッションになっている。
    ユーザが悪意のあるプログラムを作り、別のPHPユーザエディタに踏ませるとPHPエディタの開発環境をハックされる？
    Bitarrowではどうなっている？
    ページ毎にセッション(cookie)を分けることができる？


+aceが複製されるバグ
    既存ファイルの切り替え問題なし
    ファイルの作成、リネーム後に1度開いたことのあるファイルを開くとdiv(.viewer)が複製
    直後の動作だけでなく、1度ファイルを作成すると開いたことのあるファイルが全部複製されていく

    loadExplorer();が原因
        loadExplorer()内でfileInfoがリセットされるのが原因
        loadExplorerで新しく受け取ったfilelistと過去のものをマージする必要がある?
        mergeAceObjInFileList()で実装したい

        ファイル新規作成時はaceObjをマージするだけでよいが、リネームされた場合は？
        リネーム先のファイルオブジェクトにaceObjを引き継ぎたい
            リネーム処理時にFILELISTオブジェクトだけ先に書き換えておく？



ディレクトリの動作
    クリックでディレクトリ選択状態
        -ハイライトするだけ？
        +cdしたような挙動にする？
        +    ../で上の階層に戻る
    +ディレクトリの▲クリックで展開
        loadExplorer()で引数にディレクトリを指定すれば基本的にはおk
        ただその他の処理でサーバリクエスト送る際にbaseDir的なものを考慮させる必要あり
    +FILE_LISTのpathを書き換えた方が良さそう
    rename時のカレントdir考慮
    create時のカレントdir考慮
    



